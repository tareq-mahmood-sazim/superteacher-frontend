export type TSubmissionProps = {
  attachment: string[];
  createdAt: string;
  id: number;
  isLate: boolean;
  materials: number;
  submittedAt: string;
  updatedAt: string;
  userProfile: number;
};
